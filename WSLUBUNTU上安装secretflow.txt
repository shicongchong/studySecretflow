# 1. 彻底卸载所有相关包
pip uninstall -y secretflow spu secretflow-dataproxy secretflow-serving-lib secretflow_spec

# 2. 清理 pip 缓存
pip cache purge

# 3. 安装 secretflow 1.12.0b0（让 pip 自动处理依赖）
pip install secretflow==1.12.0b0 --no-cache-dir

# 4. 验证安装是否成功
python -c "import secretflow as sf; print(f'版本: {sf.__version__}')"

其他的spu总是无法初始化